[package]
name = "native-fastcdc-bench"
version = "0.1.0"
edition = "2021"

# Standalone experiment, deliberately outside the application's workspace.
[workspace]

[dependencies]
fastcdc = "=5.0.0"
sha2 = "0.10"
zstd = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
