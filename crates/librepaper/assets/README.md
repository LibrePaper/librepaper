`bootstrap.html` is the article content from `examples/bootstrap.html`, rendered
from `examples/bootstrap.qmd` with Quarto. It retains the embedded figures and
omits Quarto's navigation and scripts. The application supplies document styling.
It is checked in so release builds and new-account onboarding need no Quarto.

The other three starter sources are embedded directly from `examples/`.
